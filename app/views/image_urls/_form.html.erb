<%= form_for(@image_url) do |f| %>
  <% if @image_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_url.errors.count, "error") %> prohibited this image_url from being saved:</h2>

      <ul>
      <% @image_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %><br />
    <%= f.text_field :query %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
